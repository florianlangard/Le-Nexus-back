{% extends 'base.html.twig' %}

{% block title %}Editer un utilisateur{% endblock %}

{% block body %}
<div class="my-1 d-flex justify-content-between align-items-center px-3">
    <h1 class="align-self-center">Editer un utilisateur : {{ user.pseudo }}</h1>
    {{ include('user/_delete_form.html.twig') }}
</div>

    <div class="d-flex-inline">
    {{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    
    </div>
{% endblock %}
