{% extends 'base.html.twig' %}

{% block title %}Mood index{% endblock %}

{% block active_user %}{% endblock %}
{% block active_mood %}active{% endblock %}

{% block body %}
    <h1>Mood index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mood in moods %}
            <tr>
                <td>{{ mood.id }}</td>
                <td>{{ mood.name }}</td>
                <td>{{ mood.createdAt ? mood.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ mood.updatedAt ? mood.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('mood_show', {'id': mood.id}) }}">show</a>
                    <a href="{{ path('mood_edit', {'id': mood.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Il n'y a pas grand chose à afficher par ici...</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mood_new') }}">Create new</a>
{% endblock %}
