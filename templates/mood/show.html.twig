{% extends 'base.html.twig' %}

{% block title %}Mood{% endblock %}

{% block active_user %}{% endblock %}
{% block active_mood %}active{% endblock %}

{% block body %}
    <h1>Mood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mood.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mood.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ mood.createdAt ? mood.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ mood.updatedAt ? mood.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mood_index') }}">back to list</a>

    <a href="{{ path('mood_edit', {'id': mood.id}) }}">edit</a>

    {{ include('mood/_delete_form.html.twig') }}
{% endblock %}
