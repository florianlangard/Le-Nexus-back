{% extends 'base.html.twig' %}

{% block title %}Mood{% endblock %}

{% block active_user %}{% endblock %}
{% block active_mood %}active{% endblock %}

{% block body %}
<div class="my-1 d-flex justify-content-between align-items-center px-3">
    <h1>Infos mood : {{ mood.name }}</h1>
        <a href="{{ path('mood_new') }}" class="btn btn-success" role="button"><i class="fas fa-plus-circle"></i> Créer un mood</a>

</div>
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mood.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mood.name }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ mood.createdAt ? mood.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date de modification</th>
                <td>{{ mood.updatedAt ? mood.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex ms-0 ps-0">
    <a href="{{ path('mood_index') }}"><button class="btn btn-primary me-1">Retour à la liste <i class="fas fa-step-backward"></i></button></a>

    <a href="{{ path('mood_edit', {'id': mood.id}) }}"><button class="btn btn-warning me-1">Editer <i class="far fa-edit"></i></button></a>

    {{ include('mood/_delete_form.html.twig') }}
    </div>
{% endblock %}
